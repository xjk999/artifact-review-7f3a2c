# Model weights

Weights are not included in this repository. Place the following files in this directory before running inference:

```text
segmentation_final.pt
generation_final.pt
detection_final.pt
```

Checkpoint paths can also be changed in `config.json`.
